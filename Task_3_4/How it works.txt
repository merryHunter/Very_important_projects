Click quickly on "Mutex" ot "Mutex(using)" buttons several times.
Operation per click will be execited throw 3 seconds, so wait.
During this time try to click "Button".
"Critical code start" executes synchronously in the same UI thread.
